# Copy this file to `.env` (do NOT commit `.env`) and fill in values.
# Secrets should be configured in your deployment platform (Render) as environment variables.

# OANDA
OANDA_API_KEY=
OANDA_ACCOUNT_ID=
OANDA_ENV=practice

# Telegram
TELEGRAM_TOKEN=
CHAT_ID=

# Bot settings (optional)
SYMBOL=NAS100_USD
POSITION_SIZE=1000
FVG_BUFFER=0.5
ENTRY_START=14:30
ENTRY_END=15:00
EXIT_TIME=15:10
